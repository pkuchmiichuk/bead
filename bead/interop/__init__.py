"""Lossless interoperability mappings between bead and external schemas.

Currently provides bidirectional, law-verified lenses between bead models and
the ``layers`` linguistic-annotation schema (``bead.interop.layers``).
"""
